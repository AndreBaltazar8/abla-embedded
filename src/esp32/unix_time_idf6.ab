import "abla/unsafe/memory"

// ESP-IDF 6's picolibc configuration uses a 64-bit time_t on Xtensa.
trusted noescape extern:"c" fun time(destination: i64*): int

@inline
trusted fun unixTimeSeconds(): int = time(nativeNullPointer())
