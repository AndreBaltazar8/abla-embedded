import "../checksum/crc32.ab"
import "./ieee80211_logic.ab"
import "./ieee80211_management_logic.ab"

// IEEE 802.11 calls CRC-32/ISO-HDLC the frame check sequence (FCS).
@inline
trusted fun wifiFrameCheckSequenceNative(
    address: u32,
    length: u32
): u32 = crc32IeeeNative(address, length)
