// Pull-mode values are shared across physical GPIO and I2C expanders.
val pullNone = 0
val pullUp = 1
val pullDown = 2

fun validPullMode(mode: int): bool =
    mode == pullNone || mode == pullUp || mode == pullDown
