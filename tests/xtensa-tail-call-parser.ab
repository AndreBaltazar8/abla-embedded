import "../src/xtensa/assembly.ab"

@naked
@export("abla_xtensa_tail_probe")
trusted fun xtensaTailProbe(): void = $xtensa tail {native_tail_target}

@naked
@export("abla_xtensa_tail1_probe")
trusted fun xtensaTailOneProbe(): void = $xtensa tail1 {native_tail1_target}

fun main: int = 0
