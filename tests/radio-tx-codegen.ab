import "../src/esp32/radio/tx_esp32.ab"

@export("abla_wifi_prepare_tx_descriptor_codegen")
trusted fun prepareTx(
    descriptorAddress: u32,
    bufferAddress: u32,
    mpduLength: u32
): bool = esp32WifiPrepareTxDescriptorNative(
    descriptorAddress, bufferAddress, mpduLength
)

fun main: int = 0
