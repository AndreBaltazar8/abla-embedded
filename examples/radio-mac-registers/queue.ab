import "../../src/esp32/radio/rx_queue_esp32.ab"

@export("abla_wifi_rx_take_first")
trusted fun wifiRxTakeFirst(queueAddress: u32, maxPolls: i32): u32 =
    esp32WifiRxQueueTakeFirstNative(queueAddress, maxPolls)
