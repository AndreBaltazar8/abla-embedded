import "../../src/wifi/connection_logic.ab"

@export("abla_wifi_station_authentication_transition")
fun connectionTransitionCodegen(
    state: u32,
    algorithm: u32,
    transaction: u32,
    status: u32
): u32 = wifiStationAuthenticationTransition32(
    state, algorithm, transaction, status
)

fun main: int = 0
