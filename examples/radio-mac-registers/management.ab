import "../../src/wifi/ieee80211_management_logic.ab"

@export("abla_wifi_management_frame_valid")
trusted fun wifiManagementFrameValidNative(
    address: u32,
    length: u32
): bool = wifiManagementFrameValid32(address, length)

fun main: int = 0
